{% extends 'base.html.twig' %}

{% block title %}QuantityType index{% endblock %}

{% block body %}
    <h1>QuantityType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for quantity_type in quantity_types %}
            <tr>
                <td>{{ quantity_type.id }}</td>
                <td>{{ quantity_type.name }}</td>
                <td>{{ quantity_type.createdAt ? quantity_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ quantity_type.updatedAt ? quantity_type.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ quantity_type.status }}</td>
                <td>
                    <a href="{{ path('app_quantity_type_show', {'id': quantity_type.id}) }}">show</a>
                    <a href="{{ path('app_quantity_type_edit', {'id': quantity_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_quantity_type_new') }}">Create new</a>
{% endblock %}
